config:
  target : "http://localhost:4000"
  phases:
    - duration: 60
      arrivalRate: 1000
scenarios:
  - flow:
      - get:
          url: "/api/products"   
      - post:
          url: "/api/login"
          json:
            email: "example@gmail.com"
            password : "12345678"       